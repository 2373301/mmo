namespace = p;

// 向 data server 请求数据
xs2ds_entity_req
{
	uint64		req_guid;		// 请求的guid
	list<uint8>	data;			// 待存储的data
}

// data server 返回的结果
ds2xs_entity_ack
{
	uint64		req_guid;		// 请求的guid
	uint8		result;			// 结果
	list<uint8>	data;			// 返回的data
}